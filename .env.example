# Development
DATABASE_URL=postgres://zarnishon:zarnishon@localhost:5433/zarnishon
SESSION_SECRET=change-me-to-a-long-random-string

# Production (.env.production on the factory laptop) — generate with:
#   openssl rand -base64 32
DB_PASSWORD=
